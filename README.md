# Dockerfiles

This repository contains images that can be used to speed up the development of the polaris submarine.

Setup shell scripts haven't been tested, but the images are already build and on AUVIC's Docker Hub page.
